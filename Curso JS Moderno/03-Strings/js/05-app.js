const product = 'Lightsaber 20 Inches';

console.log(product);

// replace text on string
console.log(product.replace('Inches','"'));
console.log(product.replace('Lightsaber','Vibroblade'));

// slice to cut
console.log(product.slice(0,5));
// firs tindex is where it starts and last index is where ends the cut


// slice alternative
console.log(product.substring(0,5));